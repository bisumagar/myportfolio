export default function Footer() {
  return (
    <footer>
      <div className="wrap">
        © {new Date().getFullYear()} Bishal — built with MongoDB, Express, React &amp; Node.
      </div>
    </footer>
  );
}
